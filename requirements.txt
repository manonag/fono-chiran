# CHIRAN Context Service Dependencies
# Matches Fono's existing stack (FastAPI + asyncpg)
# MCP layer uses official Anthropic MCP SDK with Streamable HTTP transport

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
asyncpg>=0.30.0
pydantic>=2.0.0
mcp[cli]>=1.8.0
requests>=2.31.0
